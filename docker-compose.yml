version: '3.8'
services:
  backend:
    build:
      context: .
      dockerfile: Dockerfile.api
    ports:
      - "8000:8000"
    volumes:
      - ./models/saved:/app/models/saved
      - ./data/raw:/app/data/raw
    environment:
      - PORT=8000